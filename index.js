#! /usr/bin/env node
let fs = require('fs');
let util = require('./util/common');

/**
 * Function to build the final template based on inputs given by devs
 * Options:{type: 'marketing'}
 */
function buildTemplate(templateName, options){

}

module.exports = {
    build: buildTemplate
}