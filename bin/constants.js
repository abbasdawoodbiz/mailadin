const Elements = {
    BUTTON: 'button',
    TEXT: 'text',
    DIVIDER: 'divider',
    TABLE: 'table'
}

const Alignment = {
    LEFT: 'left',
    CENTER: 'center',
    RIGHT: 'right'
}

Object.freeze(Elements);
Object.freeze(Alignment);

module.exports = {
    Elements,
    Alignment
};